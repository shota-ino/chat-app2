<%# サイドバー %>

<%# 1 %>
<div class="side-bar-header">
  <div class="header-name">
    <%# <a href="#">Tom</a> %>
    <%#  表示をログインしているユーザー名、、、パスはログインしているユーザーの編集をするため%>
    <%= link_to current_user.name, edit_user_path(current_user) %>
  </div>
  <div class="create-room">
    <%# <a href="#">チャットを作成する</a> %>
    <%# rails routesで調べる %>
    <%= link_to "チャットを作成する", new_room_path %>
  </div>
</div>


<%# 2 %>
<div class="rooms">
  <%# <div class="room">
    <div class="room-name">
      <a href="#">techroom1</a>
    </div>
  </div>
  <div class="room">
    <div class="room-name">
      <a href="#">techroom2</a>
    </div>
  </div> %>

  <%# 「eachメソッド」を使って全てのチャットルームに対して同じ処理を繰り返し実行 %>
                              <%#変数名なので命名規則はないですが、今回は分かりやすいように「room」としています%>
  <% current_user.rooms.each do |room| %>
    <div class="room">
      <div class="room-name">
                      <%# room.nameの「name」は、roomsテーブルのカラム名 %>
        <%# <a href="#"><%= room.name %>
        <%# rails routes より /rooms/:room_id/messages%>
        <%# メッセージに紐付いているチャットルームのid %>
        <%= link_to room.name, room_messages_path(room) %>
      </div>
    </div>
  <% end %>
</div>